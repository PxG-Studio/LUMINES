import React from 'react';

export function InspectorPanel() {
  // Minimal stub component for tests and storybook snapshots.
  return <div data-testid="inspector-panel">Inspector Panel (stub)</div>;
}

export default InspectorPanel;

